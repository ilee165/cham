# Continue Here — Phase 3 Checkpoint A Review

Updated: 2026-08-04 (Checkpoint A complete; Checkpoint B hard stop)
Branch: `feat/phase-3-wireguard-hybrid-dns`
Base: merged `main` commit `f734d879b262e5783df25c1da00798acd7a68e7a`

## Current live state

- The East US 2 lab is retained.
- Remote Terraform state tracks 36 addresses; Azure lists 22 top-level
  resource-group resources.
- `vm-hub-ddi`, `vm-test-app`, and `vm-test-mgmt` are all
  `VM deallocated`. Do not start any VM without explicit Phase 3 Checkpoint B
  authorization.
- The four ignored per-spoke VM/NIC preservation flags are all `true`.
- `enable_private_resolver = false`; no Azure DNS Private Resolver is deployed.
- A refresh-backed Terraform plan exited `0` with `No changes`.
- The ignored home `/32` and Windows WireGuard public key still match their
  current external/public sources without exposing the values.
- The bootstrap/state resource group remains retained.
- Idle non-compute charges continue for disks, the static hub public IP,
  Private DNS, networking metadata, and state storage.

No VM was started, no Terraform apply ran, and no Azure mutation occurred
during Phase 3 planning or Checkpoint A implementation.

## Planning artifacts

- Revised implementation plan:
  `docs/superpowers/plans/2026-07-31-phase-3-wireguard-hybrid-dns.md`
- Planning research:
  `docs/superpowers/plans/2026-08-04-phase-3-wireguard-hybrid-dns-research.md`
- Plan verification:
  `docs/superpowers/plans/2026-08-04-phase-3-wireguard-hybrid-dns-verification.md`
- Checkpoint A evidence:
  `docs/evidence/phase3/checkpoint-a-readiness.md`
- Deallocation-only watchdog:
  `scripts/phase3-vm-watchdog.ps1`
- The runbook now records the actual pinned Spatium checkout/runtime.

The Phase 2 status box in README is now checked. Phase 3 remains unchecked
until live exit criteria pass.

## Material revisions

- Added a plan-review gate and a second zero-cost/read-only Checkpoint A gate.
- Limited the cost window to at most 60 minutes.
- Only the hub may start first; the app starts conditionally after hub proof.
  The management VM never starts.
- Tunnel-down and verified deallocation of all three VMs are mandatory on
  success, failure, timeout, or interruption.
- Private Resolver is removed from Phase 3 and remains disabled.
- Any Terraform drift requires a fresh saved plan/hash approval before a VM
  start.
- Hub key installation is state-aware and cannot rotate implicitly on retry.
- Evidence is boolean/narrow and excludes keys, endpoints, IDs, raw state, and
  raw plan data.
- Retain versus saved-plan destroy is a separate final decision after all VMs
  are deallocated.

## Zero-cost prerequisites resolved

1. Debian `wireguard-tools` is installed and kernel support was proved.
2. Official native Docker Engine is installed inside Debian; Docker Desktop is
   stopped, and container egress through host `wg0` was proved.
3. The pinned Spatium checkout was restored at `/opt/spatiumddi-phase3`.
4. The exact-address `172.16.0.2:53` TCP/UDP bind, scoped BIND ACLs,
   forward-only Azure zone, and local UDP/TCP answers passed.
5. The unintended WireGuard directory ACE was removed; SSH/WireGuard ACL and
   ignored-public-key comparisons passed without exposing private material.
6. A fresh disposable Kea lease produced an automatic, post-trigger DDNS
   record over UDP and TCP within the bounded poll, then the client and
   preflight artifacts were cleaned up.
7. The refresh-backed Terraform plan exited `0` with `No changes` and zero
   warnings. All three VMs remain deallocated and Private Resolver is absent.

## Next action

Review `docs/evidence/phase3/checkpoint-a-readiness.md` and the watchdog
source. If approved, explicitly authorize Checkpoint B's maximum 60-minute
window. Checkpoint B starts `vm-hub-ddi` first, starts `vm-test-app` only
after the tunnel/DNS gates pass, and never starts `vm-test-mgmt`.

Do not start a VM, apply Terraform, enable Private Resolver, or destroy
anything without the next explicit approval.

# Continue Here — Phase 2 Azure Core

Paused: 2026-08-03T10:13:25-04:00
Branch: `feat/plan-and-verify-phase-2`

## Critical live-state correction

Azure is **not empty**. At pause time, read-only checks confirmed:

- `rg-cham-lab` exists and the lab Terraform state tracks 35 resources after a partial Checkpoint C apply.
- The North Central US `Standard_B2ats_v2` hub VM and `Standard_D2als_v6` app test VM are both `VM deallocated`.
- Both test NICs and the app test VM plus its managed disk exist; the management test VM does not.
- The lab also retains one hub public IP, private DNS, networking, and a subscription budget.
- `rg-cham-tfstate` exists and the bootstrap Terraform state tracks 6 resources.
- No DNS Private Resolver resource is deployed.

After the partial Checkpoint C apply and bounded diagnostics, both running VMs were deallocated. Compute billing is stopped, but managed disks, the static public IP, Private DNS, networking usage, and retained state storage can continue to accrue charges.

## Completed work

- Checkpoint A bootstrap and recovery were explicitly approved, applied, and verified.
- Revised Checkpoint B plan `checkpoint-b-ncus-b2ats-v2.tfplan` was explicitly approved with SHA-256 `4edde8bde9dddb3a534756f177380fbd69629dbdf6feb7082cf76204fde0bfd0`.
- Checkpoint B applied 32 create, 0 change, 0 destroy.
- Live base checks passed: four peerings Connected, hub cloud-init done, BIND active, forwarding and SNAT correct, private DNS correct, NSGs correct, budget present, and Terraform drift plan clean.
- Sanitized evidence is under `docs/evidence/phase2/`.
- The original Checkpoint C plan was explicitly approved and attempted. Two NICs and the app test VM succeeded; the management test VM failed before creation.
- Available app-side checks passed for SSH/cloud-init, effective routes, hub DNS, seed resolution, and Private DNS auto-registration.

## Checkpoint C recovery gate

- The approved plan `checkpoint-c-ncus-d2als-v6.tfplan`, SHA-256 `6d532adbf8357e2bdabf6367169424cf0e43de4387d062add61df9eacee6006c`, is consumed and stale after the partial apply. Do not apply it again.
- Azure rejected `vm-test-mgmt` because Total Regional Cores reached 4 of 4. The deployed hub and app VMs require four cores even while deallocated; the management VM requires a limit of at least six.
- The two family quotas are not the blocker: BASv2 usage is 2 of 4 and Dalsv6 usage is 2 of 4.
- App egress also failed because the Standard public-IP NVA path lacked explicit hub NSG rules for forwarded traffic. The recovery configuration adds only spoke-to-Internet and spoke-to-on-premises transit rules; it does not widen home SSH or WireGuard ingress.
- Recovery plan `checkpoint-c-recovery-regional-cores-nva-nsg.tfplan` had SHA-256 `cda9d41188a4c3cb7920208a8fefbf77349d003e3672181cf463afb1325faf35` and contained 1 create, 1 in-place update, and 0 destroys. It predates the post-review fixes and is now **superseded**. Its hash authenticates only the obsolete artifact; never apply it. The local binary was renamed `SUPERSEDED-DO-NOT-APPLY-checkpoint-c-recovery-regional-cores-nva-nsg.tfplan`.
- A state-backed post-review verification plan with the app VM/NIC and management NIC preserved, management VM absent, and resolver disabled reports 0 create, 1 NSG update, 0 delete, and 0 replacement. Its SHA-256 is `73b71af977da790c2a282c2563e5ea046c1c5ce85c1e96f372a9086c1eae3c2e`. Because it was generated from an uncommitted working tree, it is verification-only and is **not approved for apply**; regenerate after commit and review the new hash.
- The explicitly approved Total Regional Cores request from 4 to 6 was accepted for processing, then reached terminal `Failed` state with `ResourceNotAvailableForOffer`; the effective limit remains 4 of 4.
- Because the user required a reported limit of at least six, the conditional gate stayed closed: the recovery plan was not applied, no VM was started, no remaining topology test ran, and no Checkpoint D destroy action occurred.

## Required next decision

The quota-request authorization has been consumed by the failed request. Before any further Azure mutation, confirm one of these paths:

1. Keep the current pause state with the hub and app VMs deallocated. This stops VM compute billing but is not a full cost stop.
2. Prepare a saved Checkpoint D destroy plan for the 35-resource partial lab stack. Do not destroy without separate hash approval. Retain or separately address the 6-resource bootstrap stack.
3. Re-plan capacity, region, or topology after resolving the offer restriction. Set app VM/NIC `true`, management NIC `true`, and management VM `false` to mirror the documented partial state, then generate and review a fresh saved plan before any apply or VM start.

## Resume preflight

1. Confirm the active Azure subscription and login.
2. Re-read both Terraform states and confirm the hub and app VMs remain deallocated; do not start them without explicit authorization.
3. Confirm the latest Total Regional Cores request remains `Failed` with `ResourceNotAvailableForOffer` and the effective limit remains 4 of 4.
4. Treat every pre-review Checkpoint C plan as superseded. After selecting a capacity/topology path, refresh state and generate a new saved plan from current `HEAD`; review its complete delta and SHA-256 before any apply.
5. Reconfirm the selected path and all saved-plan approval gates.
6. Never commit ignored backend metadata, tfvars, plan files, state, IPs, contacts, or SSH material.

Resume with: `Resume Phase 2 from .continue-here.md`.

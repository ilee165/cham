# Continue Here — Phase 2 Azure Core

Paused: 2026-08-03T00:12:13-04:00
Branch: `feat/plan-and-verify-phase-2`

## Critical live-state correction

Azure is **not empty**. At pause time, read-only checks confirmed:

- `rg-cham-lab` exists and the lab Terraform state tracks 32 resources.
- The North Central US `Standard_B2ats_v2` hub VM is running.
- The lab includes one public IP, one managed OS disk, private DNS, networking, and a subscription budget.
- `rg-cham-tfstate` exists and the bootstrap Terraform state tracks 6 resources.
- No test-VM resources and no DNS Private Resolver resources are deployed.

No Azure resource was changed, deallocated, or destroyed while pausing. The running base lab and retained state storage continue to accrue charges.

## Completed work

- Checkpoint A bootstrap and recovery were explicitly approved, applied, and verified.
- Revised Checkpoint B plan `checkpoint-b-ncus-b2ats-v2.tfplan` was explicitly approved with SHA-256 `4edde8bde9dddb3a534756f177380fbd69629dbdf6feb7082cf76204fde0bfd0`.
- Checkpoint B applied 32 create, 0 change, 0 destroy.
- Live base checks passed: four peerings Connected, hub cloud-init done, BIND active, forwarding and SNAT correct, private DNS correct, NSGs correct, budget present, and Terraform drift plan clean.
- Sanitized evidence is under `docs/evidence/phase2/`.

## Checkpoint C blocker

- `standardBasv2Family` usage is 2 of 4 vCPUs.
- The explicitly approved request for a 6-vCPU limit was accepted and then failed with `ResourceNotAvailableForOffer`.
- The test flag remains false; no Checkpoint C saved plan or test VM has been created.
- Read-only analysis found `Standard_D2als_v6` as the smallest viable x64 separate-family candidate: two vCPUs, 4 GiB, family usage 0 of 4, approximately USD 0.0804 per VM-hour.
- Changing the temporary test VMs from B2ats v2 to D2als v6 requires explicit user approval before editing inputs or generating a fresh plan.

## Required resume decision

Before any Azure mutation, confirm one of these paths:

1. Continue Checkpoint C preparation using `Standard_D2als_v6` only for the two temporary test VMs, then present a fresh saved-plan hash. Do not apply without separate hash approval.
2. Stop Phase 2 and prepare a saved destroy plan for the 32-resource lab stack. Do not destroy without separate hash approval. Retain or separately address the 6-resource bootstrap stack.
3. Temporarily deallocate only the hub VM to reduce compute cost while retaining the rest of the lab. This is not a full cost stop and requires explicit authorization.

## Resume preflight

1. Confirm the active Azure subscription and login.
2. Re-read both Terraform states and current VM power state.
3. Run a fresh Terraform drift plan.
4. Reconfirm the selected path and all saved-plan approval gates.
5. Never commit ignored backend metadata, tfvars, plan files, state, IPs, contacts, or SSH material.

Resume with: `Resume Phase 2 from .continue-here.md`.

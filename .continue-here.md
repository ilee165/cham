# Continue Here — Phase 3 Complete (B, C, and D)

Updated: 2026-08-06 (Checkpoint B passed live; Checkpoint C evidence
committed; Checkpoint D executed: lab destroyed by approved saved plan)
Branch: `feat/phase-3-wireguard-hybrid-dns`

## What happened

The operator explicitly authorized the single 60-minute Checkpoint B window,
bound to commit `53cea30a5781dc22d156aebbb111da58debe65e8` and the reviewed
watchdog hash. All 1.4 guards were re-run within five minutes of arming and
matched the Checkpoint A report. The window used roughly 25 minutes:

- Watchdog armed (deadline `2026-08-06T05:03:59Z`), hub started first.
- Hub key state `OK` with no implicit rotation; peer comparison matched.
- Every hub-only tunnel/DNS gate passed (handshakes, counters, bidirectional
  transfer addresses, direct + composed `db.azure.dwsolution.co` over UDP and
  TCP, hub reverse path, exact-address listener, home egress unchanged).
- `vm-test-app` then started; auto-registration matched the Terraform app IP
  over UDP and TCP on the first poll; app resolved the on-prem record and a
  fresh `phase3-lease-20260806041731` Kea lease over both transports.
- `vm-test-mgmt` never started.
- Mandatory closeout ran: hub `wg-quick@wg0` disabled, laptop `wg0` down,
  Spatium profiles stopped (volumes retained), all three VMs verified
  `VM deallocated` from instance view, watchdog cancelled last.

## Current live state

- Checkpoint D executed: the operator approved destroy plan SHA-256
  `900e7179…f43785`; apply removed all 36 lab resources
  (`rg-cham-lab` is gone; subscription shows only `NetworkWatcherRG` and
  `rg-cham-tfstate`).
- `rg-cham-tfstate` and its bootstrap state remain; lab state is empty.
- A fresh recreation plan generates cleanly (`36 to add`) and was reviewed
  but NOT applied. Rebuilding later requires a new saved-plan/hash approval.
- Only residual cost is the state storage account (cents/month).

## Evidence

- `docs/evidence/phase3/checkpoint-b-tunnel.md`
- `docs/evidence/phase3/checkpoint-b-dns.md`
- `docs/evidence/phase3/checkpoint-c-closeout.md`
- Runbook: proven live-window sequence added; Private Resolver session
  deferred out of Phase 3.
- Architecture: proven data path, exact-address listener, WireGuard hops.

## Next action

Phase 3 is complete and the lab is destroyed. Options from here:

1. Merge `feat/phase-3-wireguard-hybrid-dns` into `main` via PR (evidence,
   runbook, architecture, README updates).
2. Start Phase 4 (Cloudflare reconciler v2) — no Azure lab compute required.
3. Any lab rebuild needs a fresh saved plan reviewed and approved by hash
   before apply; never reuse an old plan artifact.

Do not apply Terraform or recreate infrastructure without that explicit
approval.
